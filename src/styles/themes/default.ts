export const defaultTheme = {
  white: '#FFF',
  primary: '#8257e6',
  secondary: 'orange',
}

// Creating themes:
// 1. Create a file with the themes > src / styles / themes /default.ts
// 2. Import the ThemeProvider in App.tsx
// 3. Wrap the divs with ThemeProvider and add the theme file in the property theme = { defaultTheme }
// 4. Apply the theme to the CSS.
